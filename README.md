# rename by csv



```
 [-h] [-V] [--work WORK] --input INPUT --input_csv INPUT_CSV [--output OUTPUT] [--verbose]

options:
  -h, --help            show this help message and exit
  -V, --version         show version of app
  --work WORK           Directory for work. Is prefix for all other directories that is not absolute, default ''
  --input INPUT         Path to input folder
  --input_csv INPUT_CSV
                        Path to input_csv file
  --output OUTPUT       Path for output folder, default 'output'
  --verbose             verbose output


```
